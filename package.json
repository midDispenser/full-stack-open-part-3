{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "",
  "author": "midDispenser",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../fullstackopen/part2/the-phonebook/ && npm run build && cp -r dist ../../../fullstackopen-part3/",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "mongoose": "^8.14.2",
    "morgan": "^1.10.0"
  }
}
